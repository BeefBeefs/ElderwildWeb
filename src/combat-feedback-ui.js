import{makeSprite}from'./sprite-ui.js';
const game=()=>window.__elderwildGame,view=()=>document.querySelector('#view');
const style=document.createElement('style');style.textContent=`.combat-stage{position:relative;overflow:visible}.combat-float{position:fixed;z-index:70;pointer-events:none;font-weight:900;font-size:20px;text-shadow:0 2px 2px #000;animation:combatFloat 1.45s ease-out forwards}.combat-float.enemy{color:#ff6868}.combat-float.player{color:#8ec8ff}.combat-float.xp{color:#7ee99b;font-size:13px}.combat-result{border-color:#57c879!important;margin-top:10px}.combat-result-title{display:flex;align-items:center;gap:8px;color:#7ee99b}.combat-loot{display:grid;gap:6px;margin-top:8px}.combat-loot-row{display:flex;align-items:center;gap:7px;padding:6px 8px;background:#121714;border:1px solid #2e4135;border-radius:6px}.combat-loot-row .qty{margin-left:auto;color:#72d88d}.respawn-enemy-dim{opacity:.48}.respawn-pulse{animation:respawnPulse .6s ease-in-out}@keyframes combatFloat{0%{opacity:0;transform:translateY(8px) scale(.85)}15%{opacity:1}100%{opacity:0;transform:translateY(-48px) scale(1.08)}}@keyframes respawnPulse{50%{transform:scale(1.04);color:#7ee99b}}`;document.head.append(style);
let lastResult=null;
function targetPoint(enemySide){const root=view();if(!root)return{x:innerWidth/2,y:innerHeight/2};const h=[...root.querySelectorAll('h3')].find(x=>enemySide&&game()?.state?.combat?.enemyName&&x.textContent.includes(game().state.combat.enemyName));const hp=[...root.querySelectorAll('.muted')].find(x=>x.textContent.startsWith(enemySide?'Enemy HP':'Your HP'));const r=(h||hp||root).getBoundingClientRect();return{x:r.left+r.width/2,y:r.top+Math.min(45,r.height/2)}}
function pop(text,kind,offset=0){if(!text)return;const p=targetPoint(kind==='enemy'),el=document.createElement('div');el.className=`combat-float ${kind}`;el.textContent=text;el.style.left=`${Math.max(12,p.x-18+offset)}px`;el.style.top=`${Math.max(70,p.y)}px`;document.body.append(el);setTimeout(()=>el.remove(),1500)}
window.addEventListener('elderwild-combat-feedback',e=>{const d=e.detail||{};if(d.dealt)pop(`-${d.dealt}`,'enemy');if(d.taken)pop(`-${d.taken}`,'player');if(d.styleXp)pop(`+${d.styleXp.toFixed(d.styleXp%1?1:0)} ${d.style} XP`,'xp',-55);if(d.hpXp)pop(`+${d.hpXp.toFixed(d.hpXp%1?1:0)} HP XP`,'xp',45)});
window.addEventListener('elderwild-combat-victory',e=>{lastResult=e.detail;setTimeout(decorate,0)});
function resultCard(){if(!lastResult)return null;const d=lastResult,card=document.createElement('div');card.className='card combat-result';const title=document.createElement('div');title.className='combat-result-title';const spr=makeSprite?.('enemies',d.enemyName);if(spr)title.append(spr);title.append(Object.assign(document.createElement('strong'),{textContent:`${d.enemyName} defeated!`}));card.append(title);const meta=document.createElement('small');meta.className='muted';meta.textContent=`Kill #${d.killCount}${d.autoFight?` · next fight in ${d.respawnTicks} ticks`:''}`;card.append(meta);const loot=document.createElement('div');loot.className='combat-loot';if(d.loot.length){for(const item of d.loot){const row=document.createElement('div');row.className='combat-loot-row';const icon=makeSprite?.('items',item.name);if(icon)row.append(icon);row.append(Object.assign(document.createElement('span'),{textContent:item.name+(item.upgradeLevel?` +${item.upgradeLevel}`:'')}));const qty=document.createElement('strong');qty.className='qty';qty.textContent=`×${item.quantity}`;row.append(qty);loot.append(row)}}else loot.innerHTML='<small class="muted">No drops this kill.</small>';card.append(loot);return card}
function decorate(){const root=view(),g=game();if(!root||!g)return;const c=g.state.combat;root.querySelectorAll('.combat-result').forEach(x=>x.remove());if(lastResult&&(c.respawning||(!c.active&&c.lastLog?.includes('Defeated')))){const host=root.querySelector('.auto-fight-card')||[...root.querySelectorAll('.card')].find(x=>x.querySelector('h3'));const card=resultCard();if(host&&card)host.append(card)}if(c.respawning){const h=[...root.querySelectorAll('h3,strong')].find(x=>c.respawnEnemyName&&x.textContent.includes(c.respawnEnemyName));h?.classList.add('respawn-enemy-dim');const status=[...root.querySelectorAll('p')].find(x=>x.textContent.includes('respawns in'));if(status&&!status.dataset.pulseTick){status.dataset.pulseTick=String(c.respawnTicks);status.classList.add('respawn-pulse');setTimeout(()=>status.classList.remove('respawn-pulse'),550)}}}
new MutationObserver(()=>requestAnimationFrame(decorate)).observe(document.querySelector('#view'),{childList:true,subtree:true});window.addEventListener('elderwild-game-state',decorate);decorate();
